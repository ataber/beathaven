defaults: &defaults
  adapter: postgresql
  encoding: utf8
  pool: 5
  host: localhost
  password:

development:
  <<: *defaults
  database: beathaven_development

test:
  <<: *defaults
  database: beathaven_test

production:
  <<: *defaults
  database: beathaven_production
